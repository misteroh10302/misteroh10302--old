<div id="notifyMePopup">
	
		<div class="notifyMeinner" style="display:none">
			<button id="notifyClose">x</button>
		  <h1>Keep Me Posted</h1>
		 	 {% assign featured_image = current_variant.featured_image | default: product.featured_image %}

	          <div class="product__photo"  data-thumb="{{ featured_image.src | img_url: 'small_cropped' }}">
	            <img src="{{ featured_image | img_url: '1024x1024' }}" alt="{{ image.alt | escape }}" data-image-id="{{ featured_image.id }}" class="productImg">
	          </div>
			 <div class="lowertext">
			 	 <h2>THIS ITEM IS CURRENTLY OUT OF STOCK.</h2>
			 	 <h2>LEAVE US YOUR INFO AND WE’LL NOTIFY YOU WHEN THE ITEM BECOMES AVAILABLE.</h2>
			 </div>
			
				<!-- <div class="top-item">
					<label for="color">Color</label>
					<input id='color' type="text">
				</div>
				<div class="top-item">
					<label for="size">Size</label>
					<input id='size' type="text">
				</div> -->
				<label for="email">email</label>

		          <input type="email" id="notify_email">
		          <!-- <div id="notifyMe">Notify Me</div>-->
         		 <button id="notify_button" class="sendMeProd">Send</button>

		<div class="gallery">
		  <div class="featured">
		  {% for product in collection.products %}
		    {% if product.tags contains 'bags for tots' %}
		      {{ product.title }}
		    {% endif %}
		  {% endfor %}
		  </div>
		</div>
		</div>
	
</div>

<script>  

$('#notify_button').click(function(e) {
    e.preventDefault();
    $(this).addClass('sendChange');
    $(this)[0].innerHTML = "We will keep you updated!";
    var email = $('#notify_email').val(),
        productId = {{product.id}},  // rendered by Liquid
        variantId = {{product.variants.first.id}}; // rendered by Liquid
    	BIS.create(email, variantId, productId); // create the notification

 
  });


</script>