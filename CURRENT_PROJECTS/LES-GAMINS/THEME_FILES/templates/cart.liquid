<!-- /templates/cart.liquid -->
{% comment %}

  This is your /cart template. If you are using the Ajaxify Cart plugin,
  your form (with action="/cart") layout will be used in the drawer/modal.

  For info on test orders:
    - General http://docs.shopify.com/manual/your-store/orders/test-orders
    - Shopify Payments - http://docs.shopify.com/manual/more/shopify-payments/testing-shopify-payments

{% endcomment %}
{% if cart.item_count > 0 %}
<div class="wrapper page-margin">
  <h3>{{ 'cart.general.title' | t }}</h3>
  <form action="/cart" method="post" novalidate class="cart">
    <div class="cart-padding">
      <h3 id="ajaxCartHeader">{{ 'cart.general.title' | t }}</h3>
    <div class="cart__body">
      <div class="cart__row medium-down--hide cart__header-labels">
        <div class="grid--full">
          <div class="grid__item large--one-half push--large--one-half">
            <div class="grid--full">
              {% if settings.product_quantity %}
                <div class="grid__item one-third medium-down--one-third">
                  {{ 'cart.label.price' | t }}
                </div>
                <div class="grid__item one-third medium-down--one-third text-center">
                  {{ 'cart.label.quantity' | t }}
                </div>
              {% else %}
                <div class="grid__item two-thirds medium-down--two-thirds"></div>
              {% endif %}
              <div class="grid__item one-third medium-down--one-third text-right">
                {{ 'cart.label.total' | t }}
              </div>
            </div>
          </div>
        </div>
      </div>
      {% comment %}
        Loop through products in the cart
      {% endcomment %}
      {% for item in cart.items %}
        <div class="cart__row" data-id="{{ item.id }}">
          <div class="grid--full cart__row--table-large">

            <div class="grid__item large--one-half">
              <div class="grid--full cart__row--table-large">

                <div class="grid__item one-third">
                  <a href="{{ item.url | within: collections.all }}" class="cart__image">
                    <img src="{{ item | img_url: 'medium' }}" alt="{{ item.title | escape }}">
                  </a>
                </div>

                <div class="grid__item two-thirds cart__item__title">
                  <a href="{{ item.url }}" class="h4--body">
                    {{ item.product.title }}
                  </a>


                  {% unless item.variant.title contains 'Default' %}
                    <p><small>{{ item.variant.option1 }}</small> <span class="unicode">•</span>  <small>SIZE {{ item.variant.option2 }}</small></p>

                  {% endunless %}

                  {% if settings.cart_vendor_enable %}
                    <p>{{ item.vendor }}</p>
                  {% endif %}



                  <a href="/cart/change?line={{ forloop.index }}&amp;quantity=0" class="cart__remove uppercase lighten" data-id="{{ item.id }}">
                    <small>{{ 'cart.general.remove' | t }}</small>
                  </a>

                   <!-- <div class="giftWrapInd">
                     {% assign property_size = item.properties | size %}
                      {% if property_size > 0 %}
                        {% for p in item.properties %}
                          {% unless p.last == blank %}
                           <small> {{ p.first }}: </small>
                            {% comment %}
                              Check if there was an uploaded file associated
                            {% endcomment %}
                            {% if p.last contains '/uploads/' %}
                              <a href="{{ p.last }}">{{ p.last | split: '/' | last }}</a>
                            {% else %}
                              {{ p.last }}
                            {% endif %}
                          {% endunless %}
                        {% endfor %}
                      {% endif %}
                    <input type="checkbox" id="test1" />
                    <label for="test1">Is this a gift?</label>
                  </div> -->
                </div>


              </div>


            </div>

            <div class="grid__item large--one-half">
              <div class="grid--full cart__row--table-large">
                {% if settings.product_quantity %}
                  <div class="grid__item one-third text-left">
                    <span class="cart__mini-labels">{{ 'cart.label.price' | t }}</span>
                    <span class="h5--body money">{{ item.price | money }}</span>
                  </div>

                  <div class="grid__item one-third text-center">
                    <span class="cart__mini-labels">{{ 'cart.label.quantity' | t }}</span>
                    {% comment %}
                      Added data-id for the ajax cart implementation only.
                    {% endcomment %}
                    <input type="number" name="updates[]" id="updates_{{ item.id }}" value="{{ item.quantity }}" min="0" data-id="{{ item.id }}" data-submit="true" class="ajaxcart--hide">
                  </div>
                {% else %}
                  <div class="grid__item two-thirds"></div>
                {% endif %}

                <div class="grid__item one-third text-right">
                  <span class="cart__mini-labels">{{ 'cart.label.total' | t }}</span>
                  <span class="h5--body money">{{ item.line_price | money }}</span>
                </div>
              </div>

              <div class="ajaxcart__errors hidden errors text-center" id="ajaxcart__item__{{item.id}}__errors">
                {{ 'cart.general.stockout' | t }}
              </div>
            </div>

          </div>
        </div>
      {% endfor %}
    </div>


  </div> <!-- end padding -->

    <div class="cart__row cart__footer">
      <div class="grid">
        {% comment %}
          Optional, add a textarea for special notes
            - Your theme settings can turn this on or off. Default is on.
            - Make sure you have name="note" for the message to be submitted properly
        {% endcomment %}

        <div class="grid__item {% if settings.cart_notes_enable %} large--one text-right{% endif %}">
          <p class="cart__footer__text">
            <span class="cart__subtotal-title uppercase">{{ 'cart.general.subtotal' | t }}</span>
            <span class="h3--body cart__subtotal money">{{ cart.total_price | money }}</span>
          </p>
          <p class="cart__footer__text"><em>{{ 'cart.general.shipping_at_checkout' | t }}</em></p>

          <input type="submit" name="update" class="btn--secondary update-cart btn--large uppercase" value="{{ 'cart.general.update' | t }}">
          <span><input type="submit" name="checkout" class="btn uppercase btn--large checkout__button" value="{{ 'cart.general.checkout' | t }}"></span>
        </div>

       <div class="desktop">
        {% include 'gift-wrapping' %}
      </div>
       <div class="mobile mobile-gift" style="">
         <div class="gift-mobile">Gift Settings +</div>
         <div class="gift-drawer">
           {% include 'gift-wrapping0' %}
         </div>
       </div>
        <button class="desktop" id="tots-click">{{settings.cart_popup}}</button>
        <div class="mobile mobile-cart" id="tots-fold">{{settings.cart_popup}} +</div>
         <div class="tots-mobile-drawer" style="display:none">
            <div class="join-us-tots">
               <h1> {{page.wear-share-recycle.title}}</h1>
                <img src="{{ 'refferal_image.png' | asset_url }}" alt="Bags For Tots">
                 {{pages.wear-share-recycle-mobile.content}}
               <button id="join-tots">
                {{settings.wear-share}}
               </button>
            </div>
            <script>
              var theCount = 0;
              $('#join-tots').on('click',function(){
                  if (theCount>=1) return;
                  else {
                    Shopify.addItem(35369401927,1);
                    Shopify.onItemAdded();
                    $('#join-tots').html('Thank You!');
                    $('#join-tots').addClass('join-blue');
                  }
                });
            </script>
         </div>

        <a id="more-bags-info" href="/pages/about-us#from-cart">More Info Here</a>

         {% if settings.cart_notes_enable %}
          <div class="grid__item large--one cart__instructions text-right" >
            <label class="cart__instructions__label" for="CartSpecialInstructions">Special Instructions</label>
            <textarea name="note" class="input-full" id="CartSpecialInstructions">{{ cart.note }}</textarea>
          </div>
        {% endif %}



        {% if additional_checkout_buttons and settings.cart_show_additional_buttons %}
          <div class="grid__item additional-checkout-buttons">
            {{ content_for_additional_checkout_buttons }}
          </div>
        {% endif %}

      </div>

    </div>

  </form>

   <div class="wrapper page-margin continue-button">
      <div id="continue-cart">  <a href="{{shop.url}}/collections/all"><button class="btn">Continue Shopping</button></a></div>
    </div>

</div>

{% else %}
  {% comment %}
    The cart is empty
  {% endcomment %}
  <div class="wrapper page-margin text-center">
    <h3 class="uppercase h3">{{ 'cart.general.title' | t }}</h3>
    <p>{{ 'cart.general.empty' | t }}</p>
    <a href="{{ settings.cart_continue_browsing }}" class="btn btn--large">{{ 'cart.general.continue_browsing_html' | t }}</a>
  </div>
{% endif %}

<script>
  $('.clickkkkk').on('click',function(e){
    e.preventDefault;
    var theVarId = $(this).closest('.cart__row').attr('data-id');
    console.log(theVarId);
    // jQuery.post('/cart/cahnge.js', {updates: {theVarId: 2, properties:}});
    $.ajax({
        type: 'POST',
        url: '/cart/change.js',
        dataType: 'json',
        data: {
            id: theVarId
        },
         success: function(cart) {
                this_item.remove();
            },
            error: function(response) {
                alert(response);
            }
        });
 });

// Mobile bags for tots drawer
  $('#tots-fold').on('click',function(e){
     e.preventDefault;
      $('.tots-mobile-drawer').toggle();
  });


  $('.gift-mobile').on('click',function(e){
     e.preventDefault;
      $('.gift-drawer').toggle();
  });
 $('#giftmessage').on('click',function(){
    $('#gift-note').toggle();
 });

</script>
